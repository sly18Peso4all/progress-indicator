import React from "react";
import ReactDOM from "react-dom";

import ProgressIndicator from "./App";

ReactDOM.render(
  <React.StrictMode>
    <ProgressIndicator />
  </React.StrictMode>,
  document.getElementById("root")
);
